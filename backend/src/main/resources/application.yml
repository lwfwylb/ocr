server:
  port: 8889

spring:
  application:
    name: intelligent-extraction-backend
  profiles:
    active: mysql
  jackson:
    default-property-inclusion: non_null
    time-zone: Asia/Shanghai
    deserialization:
      fail-on-unknown-properties: false
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 120MB
  mvc:
    log-request-details: true

mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true

app:
  cors:
    allowed-origins:
      - http://127.0.0.1:5170
      - http://localhost:5170
  storage:
    type: ${APP_STORAGE_TYPE:local}
    upload-dir: ${APP_STORAGE_UPLOAD_DIR:data/uploads}
    artifact-dir: ${APP_STORAGE_ARTIFACT_DIR:data/artifacts}
    ocr-preview-dir: ${APP_STORAGE_OCR_PREVIEW_DIR:data/ocr-preview}

logging:
  level:
    root: info
    web: debug
    sql: debug
    com.example.reits.backend.mapper: debug
    org.mybatis: debug
    org.apache.ibatis: debug
    org.springframework.web: debug
    org.springframework.web.servlet.DispatcherServlet: debug
