<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.extraction.mapper.PromptTemplateMapper">
    <resultMap id="PromptTemplateRecordMap" type="com.example.extraction.model.domain.PromptTemplateRecord">
        <id property="id" column="id"/>
        <result property="templateType" column="template_type"/>
        <result property="templateName" column="template_name"/>
        <result property="templateContent" column="template_content"/>
        <result property="status" column="status"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="BaseColumns">
        id, template_type, template_name, template_content, status, updated_by, created_at, updated_at
    </sql>

    <select id="selectByType" resultMap="PromptTemplateRecordMap">
        select <include refid="BaseColumns"/>
        from prompt_template_config
        where template_type = #{templateType}
    </select>

    <insert id="insert" parameterType="com.example.extraction.model.domain.PromptTemplateRecord">
        insert into prompt_template_config (
            id, template_type, template_name, template_content, status, updated_by, created_at, updated_at
        ) values (
            #{id}, #{templateType}, #{templateName}, #{templateContent}, #{status}, #{updatedBy}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="update" parameterType="com.example.extraction.model.domain.PromptTemplateRecord">
        update prompt_template_config
        set template_name = #{templateName},
            template_content = #{templateContent},
            status = #{status},
            updated_by = #{updatedBy},
            updated_at = #{updatedAt}
        where template_type = #{templateType}
    </update>
</mapper>
